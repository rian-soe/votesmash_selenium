# Application Configuration
APP_URL=http://localhost:8080
BROWSER=chrome
HEADLESS=false
WINDOW_SIZE=1920,1080
REMOTE_URL=
DB_DISABLED=false

# Database Configuration (Direct Connection)
DB_HOST=localhost
DB_USER=sail
DB_PASSWORD=password
DB_NAME=votesmash
DB_PORT=3308

# SSH Tunnel Configuration (for remote database access)
SSH_HOST=13.51.177.101
SSH_PORT=22
SSH_USERNAME=ubuntu
SSH_PRIVATE_KEY_PATH=/path/to/your/private/key.pem

# Remote Database Configuration (accessed via SSH tunnel)
DB_REMOTE_HOST=127.0.0.1
DB_REMOTE_PORT=3306
DB_REMOTE_USER=user
DB_REMOTE_PASSWORD=password
DB_REMOTE_NAME=votesmash

# Driver Configuration - Selenium WebDriver now manages drivers automatically
# No need to specify driver paths - Selenium handles driver management

# Tour Phase Durations (in seconds)
TOUR_PHASE_1_DURATION=60
TOUR_PHASE_2_DURATION=180
TOUR_PHASE_3_DURATION=420
TOUR_PHASE_4_DURATION=2700

# CI/CD Environment
CI=false
